# Secrets and state — never commit
terraform.tfvars
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
build/
crash.log
# Saved plan files bake in variable values (secrets) — never commit
tfplan
*.tfplan
