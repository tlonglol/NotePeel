# API Framework
fastapi==0.102.0
uvicorn==0.23.2          # no [standard] extras (uvloop/watchfiles) — unused under Mangum on Lambda
python-multipart>=0.0.6

# Lambda ASGI adapter
mangum>=0.17.0

# Environment
python-dotenv==1.0.1

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Authentication
pyjwt[crypto]>=2.8.0
bcrypt>=4.1.0
google-auth>=2.0.0

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Gemini AI
google-genai
 
# AWS S3 object storage (also supports S3-compatible endpoints)
boto3>=1.28.0

# Image Processing
Pillow>=10.0.0
pillow-heif>=0.16.0      # HEIC decoding (cross-platform; replaces macOS-only `sips`)
